# ============================================
# MongoDB Configuration
# ============================================
# MongoDB connection string
# Format: mongodb://[username:password@]host[:port][/database][?options]
# mongodb://localhost:27017/genitofashion-db
MONGODB_URI=mongodb://localhost:27017/genitofashion-db

# ============================================
# NextAuth.js Configuration
# ============================================
# Secret key for JWT encryption (generate a random string)
# You can generate one using: openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-key-here-change-this-in-production

# Base URL of your application (optional, defaults to http://localhost:3000 in development)
NEXTAUTH_URL=http://localhost:3000

# ============================================
# Cloudinary Configuration
# ============================================
# Your Cloudinary cloud name
CLOUDINARY_CLOUD_NAME=your-cloudinary-cloud-name

# Your Cloudinary API key
CLOUDINARY_API_KEY=your-cloudinary-api-key

# Your Cloudinary API secret
CLOUDINARY_API_SECRET=your-cloudinary-api-secret

# ============================================
# Google Cloud Vision API Configuration
# ============================================
# Option 1: Service Account (Recommended for production)
# Path to your Google Cloud service account JSON key file
# Example: /Users/username/path/to/service-account-key.json
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# Your Google Cloud project ID
GOOGLE_CLOUD_PROJECT_ID=your-google-cloud-project-id

# Option 2: API Key (Alternative method, simpler for development)
# If using API key instead of service account, uncomment the line below
# and comment out GOOGLE_APPLICATION_CREDENTIALS above
# GOOGLE_CLOUD_VISION_API_KEY=your-google-cloud-vision-api-key

# ============================================
# OpenAI Configuration (Optional)
# ============================================
# OpenAI API key for GPT-4 Vision descriptions
# Only needed if you want to use GPT-4 for generating product descriptions
# Leave empty if you only want to use Google Vision API
OPENAI_API_KEY=sk-your-openai-api-key-here

# ============================================
# Notes:
# ============================================
# 1. Copy this file to .env.local and replace all dummy values with your actual credentials
# 2. Never commit .env.local to version control (it's in .gitignore)
# 3. For production, use environment variables in your hosting platform
# 4. Google Cloud: Choose either GOOGLE_APPLICATION_CREDENTIALS (service account)
#    OR GOOGLE_CLOUD_VISION_API_KEY (API key), not both
# 5. OpenAI API key is optional - only needed for GPT-4 descriptions
